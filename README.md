# solar-flare-prediction-deep-learning
Bachelor's Thesis (TFG). Solar flare forecasting using Multi-Layer Perceptrons (MLP). The project compares two training strategies: Pre-Binary (grouping flare classes before training) and Post-Binary (multi-class training followed by binary grouping). Features a full pipeline from NOAA/GOES data ingestion to performance metric reporting.
